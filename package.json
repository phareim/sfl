{
  "name": "sfl",
  "private": true,
  "workspaces": [
    "api",
    "web",
    "chrome-extension"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel --if-present dev"
  },
  "engines": {
    "node": ">=20"
  }
}
