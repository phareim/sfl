{
  "name": "sfl-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute sfl-db --file=src/db/schema.sql",
    "db:init:local": "wrangler d1 execute sfl-db --local --file=src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.12.2",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
